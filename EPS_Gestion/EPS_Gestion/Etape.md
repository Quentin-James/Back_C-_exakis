ng generate component home --inline-template --skip-tests // Créer un composant home sans fichier de test
ng serve // Lancer le serveur de développement
import injectable// Importer le décorateur injectable il sert a injecter un service dans un composant
